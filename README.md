# GitPipe
```
git pull
git checkout -b {update-branch-name}
git push --set-upstream origin {update-branch-name}
git add .
git commit -m "description"
git push
{ Perform pull request on Github }
git checkout main (or master)
git branch -D {update-branch-name}
git pull
```
